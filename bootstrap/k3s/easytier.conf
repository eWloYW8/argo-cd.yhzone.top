[Unit]
Wants=easytier.service
After=easytier.service network-online.target

[Service]
ExecStartPre=/bin/sh -c 'for i in $(seq 1 60); do ip -4 addr show dev tun0 2>/dev/null | grep -q "inet 10.1.2.4/" && exit 0; sleep 1; done; exit 1'
