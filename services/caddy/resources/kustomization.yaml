apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: platform-certificates
resources:
- certificate.yaml
- deployment.yaml
configMapGenerator:
- name: caddy-internal
  files:
  - Caddyfile
  - run.sh
generatorOptions:
  disableNameSuffixHash: true
