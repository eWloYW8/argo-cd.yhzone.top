# 仓库约定

- 本仓库公开，禁止提交凭据、私钥、kubeconfig 或渲染后的 Secret。
- 文档仅说明仓库用法，不列服务清单、节点地址、拓扑、登录方法、备份路径或恢复细节。运维资料放私有仓库，不为每个应用新增 README。
- 一个应用一个目录；沿用现有 Kustomize、Helm 和 ApplicationSet 结构。固定版本，完整 values 文件名带 Chart 版本，下载缓存不提交。
- 变更前检查私有运维约定。保留镜像前缀和基础组件的启动依赖边界；不从参考仓库复制环境配置。
- kubectl 写操作显式选择目标 context；不改错集群，不擅自删除数据、凭据或改变网络边界。
- 仅配置变更需要重新渲染校验；不公开包含凭据的输出。使用 Conventional Commits。
- 文字简短，不重复配置内容，不写迁移流水账或模板化总结。
